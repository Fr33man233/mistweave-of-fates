# Mistweave of Fates V0.4 验收与发布记录

> 状态：V0.4 本地封闭纵向切片已完成；不代表 MVP、公测或正式上线。本文件仅记录本地未提交证据。
> 日期：2026-08-21

## 已验证

- Checkpoint A：`deepseek-v4-flash` 的 T0/T1/T2 已有独立证据；此前用户授权的 T3 最小廷根场景也已完成结构化 Keeper 链路验证。
- Checkpoint B/C 离线规则：COC 7e 最小检定、灵性/SAN 投影、固定 seed、代表案件、职业/路径透镜、世界 AI 提案边界、占卜家能力白名单均有测试。
- React/桌面网页：V0.4 入口、COC 建角、必选性别、廷根河岸、自由行动、行动预览、撤回、确认、确定性结果、即时反馈、Keeper 叙事、退出和返回 V0.3 已实测。
- 人物档案建角：界面使用“创建人物档案”；属性以中文名称显示；四职业使用不同属性模板并保留 100 点自由属性点；新建界面仅提供女性/男性且不改变属性（旧快照值仅兼容解析）；职业技能点 = 教育值 × 20，兴趣技能点 = 智力值 × 10，档案页显示当前切片技能数值；随机分配和平均分配预设可复现且加点数值实时更新。
- 存档：独立 IndexedDB 命名空间、current/previous 校验恢复、刷新继续、未提交候选不重放、重置清除快照已覆盖测试；写入、清空和读取现在串行化，避免卸载保存与重置竞态。
- 隐私：保存的模型交互记录只包含 requestId、用途、模型/Prompt 版本、上下文哈希、延迟、分项 token、错误码和事件引用，不保存原始自由文本、Prompt、隐藏真相或密钥。
- 服务：模型网关非模型请求继续交给 Vite；请求体限长、用途白名单、超时、单次 JSON 重试、并发上限和标准错误映射均有测试。
- 本轮阻断项修复：主界面支持最多三个人物档案的独立创建与切换；切换会丢弃未提交候选但保留共享场景与各自人物数据。
- 本轮模型失败链：浏览器夹具验证解释失败后的重试与确定性降级，失败和成功记录均只持久化脱敏元数据；规则结算与叙事失败可分离恢复。
- 本轮世界边界：本地环境提案只能引用可见对象、允许模板和短期检查点；提案不会写隐藏真相、直接提交事件或替换确定性规则。
- 服务端遥测：模型网关现在写入本地有界 JSONL 元数据账本（请求 ID、用途、模型、Prompt 版本、结果、错误码、延迟、分项 token）；不保存请求正文、候选正文、个人数据或密钥，写入失败不阻塞游戏。
- 异常与恢复矩阵：桌面 Chromium 已覆盖非法候选、模型离线/失败、重复确认、刷新、旧 V0.3 数据库隔离和 current 损坏回退 previous；移动端、Firefox/WebKit 与视觉基线按用户决定延期。
- 真实世界网关：`world-v2` 已在桌面浏览器只调用一次并通过；提案进入临时状态，刷新后恢复，模型交互元数据为 1 条且不重放请求。
- 自动化证据：全量 Vitest 与桌面 Playwright 均覆盖上述离线链；真实调用只通过独立脚本执行，未进入自动化测试。
- 占卜能力任务链：V0.4 档案预置占卜家序列 9；具体调查中可选择预兆窥视或灵性直觉，进入行动预览后确认；规则核心扣除 1 点灵性并记录奖励骰/信息层级/额外行动，灵性直觉可解锁沿水痕继续追踪；刷新恢复能力结算结果且不重放请求。
- 本轮最终运行：Vitest 34 个测试文件/131 个测试通过；桌面 Chromium Playwright 18/18 通过；TypeScript 构建与生产边界检查通过；`http://127.0.0.1:5175/` 返回 HTTP 200。

## 2026-08-21 本地验收与发布结论

- 验收范围：V0.4 本地封闭纵向切片，不包含公网部署、生产遥测收集器或未参与实现者试玩。
- 自动化验收：`pnpm loop:fast` 通过（34 个 Vitest 测试文件、131 项测试、TypeScript 构建、生产边界检查）；`pnpm test:e2e` 通过（桌面 Chromium 18/18）。
- 服务验收：`http://127.0.0.1:5175/` 返回 HTTP 200。
- 结论：**GO——V0.4 本地封闭纵向切片可验收并可标记为已完成。**
- 发布边界：本次只完成本地验收记录和本地发布状态更新；不创建提交、不推送 GitHub、不宣称 MVP、公测或正式上线。

## V0.4 范围外延期项

1. 未参与实现者的独立试玩不纳入 V0.4，移至后续内容足够成熟的版本；V0.5 MVP 候选再承担外部体验验证。
2. 公网生产环境、公网 AI 网关和生产长期遥测收集器不纳入 V0.4，移至 V0.9/V1.0 部署与公测门；V0.4 仅保留本地有界服务端遥测账本。

## 资源与安全边界

- 本轮新增真实模型调用均在用户明确授权的最小范围内：真实 Keeper 叙事和一次世界 AI 提案；未扩大 T3 数据边界或发送隐藏真相、源码、完整存档、个人数据。
- 本轮真实世界 AI 提案通过可见对象、允许模板和检查点校验；仍只进入临时状态，不直接提交事件或替换确定性规则。
- 2026-08-21 已按授权执行世界提案 T3 增量验证；前 4 次失败/空结果均保留为证据，新授权第 5 次以 `max_tokens=512`、关闭思考和精确 JSON 示例通过。累计 T3 7/200，所有调用均未保存原始候选正文。
- 随后经用户单独授权，浏览器真实网关调用 1 次并通过，累计 T3 8/200；本轮人工复核进一步证明 Keeper/世界 AI/占卜能力的单次连续链路可运行。该证据用于 V0.4 本地封闭切片，不替代后续版本的独立试玩。
- 未提交、未推送；旧 V0.1–V0.3 存档命名空间未被覆盖。
- V0.4 可以标记为“本地封闭纵向切片已完成”，但不得称为 MVP、内测、公测或正式上线；后续版本仍需按路线文件完成外部体验和公网部署门。
